stages:
  - build
  - deploy

cache:
  paths:
    - node_modules/

build:
  image: npm:latest
  stage: build
  script:
    - npm run build
  artifacts:
    paths:
      - dist/

deploy:
  image: npm:latest
  stage: deploy
  script:
    - npm install -g firebase-tools
    - firebase deploy --token $FIREBASE_TOKEN
  only:
    - main
